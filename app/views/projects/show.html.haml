%p#notice= notice

%p
  %b Name:
  = @project.name
%div
  - @project.projectsUser.each do |projectUser|
    %div 
      = projectUser.role

= link_to 'Edit', edit_project_path(@project)
\|
= link_to 'Back', projects_path
